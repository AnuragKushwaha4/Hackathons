UseCallback hook : This hook is provided by react that cache function reference and prevent unnecessary
re render of child component when a function in parent component passed as a  prop then if parent
component re render the function defination is redefined so child component also re render.

Another usecase of useCall back function is handleing expensive function as it freeze re creation of 
function at